# Reconstructed assets

This project recreates the reference frame procedurally in `main.js` using pixel blocks and a constrained palette.

- `palette.json`: extracted/reduced color set used for all scene drawing.
- `layout.json`: bounding boxes for major visual regions from the reference.

All rendering uses nearest-neighbor pixel drawing with smoothing disabled.
